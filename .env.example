# Environment Variables Template
# Copy this file to .env.local and replace with your values
# DO NOT commit .env.local to version control

# ==============================================
# LOCAL DEVELOPMENT (Using Docker Supabase)
# ==============================================
# Get these values by running: supabase status
# Or use the defaults shown below for local Docker:

# Frontend (Vite requires VITE_ prefix)
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=your-local-anon-key

# Backend (if needed for server-side operations)
SUPABASE_URL=http://localhost:54321
SUPABASE_SERVICE_KEY=your-local-service-key
DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres

# ==============================================
# PRODUCTION (Never store these values locally!)
# ==============================================
# Production values should be configured in:
# - GitHub Secrets (for CI/CD)
# - AWS Amplify Environment Variables (for frontend)
# - Supabase Dashboard (for backend)
#
# NEVER create a .env file with production values
# NEVER store these locally:
# - SUPABASE_DB_PASSWORD
# - RESEND_API_KEY
# - Production SUPABASE_SERVICE_KEY

# ==============================================
# APPLICATION SETTINGS
# ==============================================
PORT=4010
VITE_PORT=4011
NODE_ENV=development
VITE_APP_ENV=development