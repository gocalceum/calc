{
  "name": "calc-server",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=../.env --watch index.ts",
    "start": "bun --env-file=../.env index.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.54.0",
    "resend": "^6.0.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}