{
  "name": "calc",
  "type": "module",
  "private": true,
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "bun run dev:frontend & bun run dev:server",
    "dev:frontend": "cd frontend && bun run dev",
    "dev:server": "cd server && bun run dev",
    "dev:local": "export $(cat .env.local | xargs) && bun run dev",
    "install:all": "bun install && cd frontend && bun install && cd ../server && bun install",
    "build": "cd frontend && bun run build",
    "db:setup": "./scripts/setup-db.sh",
    "db:push:prod": "echo 'Production migrations should be run via GitHub Actions. Use: gh workflow run database-migration.yml'",
    "supabase:start": "./scripts/start-local.sh",
    "supabase:stop": "supabase stop",
    "supabase:status": "supabase status",
    "supabase:reset": "supabase db reset",
    "prepare": "husky"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "tsx": "^4.20.3"
  }
}
