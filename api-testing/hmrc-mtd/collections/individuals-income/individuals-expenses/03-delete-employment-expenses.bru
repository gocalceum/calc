meta {
  name: Delete Employment Expenses
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/individuals/expenses/employments/{nino}/{taxYear}
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Delete Employment Expenses
  
  
  
  API: Individuals Expenses
}
