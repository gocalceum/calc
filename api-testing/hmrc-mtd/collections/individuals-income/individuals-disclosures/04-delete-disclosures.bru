meta {
  name: Delete Disclosures
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/individuals/disclosures/{nino}/{taxYear}
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Delete Disclosures
  
  
  
  API: Individuals Disclosures
}
