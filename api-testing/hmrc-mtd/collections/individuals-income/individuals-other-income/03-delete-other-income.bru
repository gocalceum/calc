meta {
  name: Delete Other Income
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/individuals/other-income/{nino}/{taxYear}
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Delete Other Income
  
  
  
  API: Individuals Other Income
}
