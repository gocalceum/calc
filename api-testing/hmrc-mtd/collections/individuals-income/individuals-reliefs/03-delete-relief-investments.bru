meta {
  name: Delete Relief Investments
  type: http
  seq: 3
}

delete {
  url: {{base_url}}/individuals/reliefs/investment/{nino}/{taxYear}
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Delete Relief Investments
  
  
  
  API: Individuals Reliefs
}
