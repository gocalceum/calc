meta {
  name: Retrieve Other Reliefs
  type: http
  seq: 4
}

get {
  url: {{base_url}}/individuals/reliefs/other/{nino}/{taxYear}
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Retrieve Other Reliefs
  
  
  
  API: Individuals Reliefs
}
