meta {
  name: Delete Foreign Reliefs
  type: http
  seq: 9
}

delete {
  url: {{base_url}}/individuals/reliefs/foreign/{nino}/{taxYear}
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Delete Foreign Reliefs
  
  
  
  API: Individuals Reliefs
}
