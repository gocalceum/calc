meta {
  name: Delete Pensions Reliefs
  type: http
  seq: 12
}

delete {
  url: {{base_url}}/individuals/reliefs/pensions/{nino}/{taxYear}
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Delete Pensions Reliefs
  
  
  
  API: Individuals Reliefs
}
