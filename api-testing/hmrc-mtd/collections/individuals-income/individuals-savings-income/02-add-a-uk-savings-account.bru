meta {
  name: Add a UK Savings Account
  type: http
  seq: 2
}

post {
  url: {{base_url}}/individuals/savings-income/uk-accounts/{nino}
  body: json
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Add a UK Savings Account
  
  
  
  API: Individuals Savings Income
}
