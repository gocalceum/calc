meta {
  name: Delete Other Capital Gains and Disposals
  type: http
  seq: 8
}

delete {
  url: {{base_url}}/individuals/disposals-income/other-gains/{nino}/{taxYear}
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Delete Other Capital Gains and Disposals
  
  
  
  API: Individuals Capital Gains Income
}
