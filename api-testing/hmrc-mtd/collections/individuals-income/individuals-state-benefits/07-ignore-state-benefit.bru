meta {
  name: Ignore State Benefit
  type: http
  seq: 7
}

post {
  url: {{base_url}}/individuals/state-benefits/{nino}/{taxYear}/{benefitId}/ignore
  body: json
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Ignore State Benefit
  
  
  
  API: Individuals State Benefits
}
