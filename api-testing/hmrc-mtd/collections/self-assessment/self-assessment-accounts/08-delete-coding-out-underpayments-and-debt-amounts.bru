meta {
  name: Delete Coding Out Underpayments and Debt Amounts
  type: http
  seq: 8
}

delete {
  url: {{base_url}}/accounts/self-assessment/{nino}/{taxYear}/collection/tax-code
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Delete Coding Out Underpayments and Debt Amounts
  
  
  
  API: Self Assessment Accounts
}
