meta {
  name: Update Accounting Type
  type: http
  seq: 5
}

put {
  url: {{base_url}}/individuals/business/details/{nino}/{businessId}/{taxYear}/accounting-type
  body: json
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
  
  test("Should have correlation ID", function() {
    expect(res.headers["x-correlationid"]).to.be.a("string");
  });
}

docs {
  ## Update Accounting Type
  
  
  
  ### Path Parameters

}
