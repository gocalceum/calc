meta {
  name: Delete Other Expenses
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/individuals/expenses/other/{nino}/{taxYear}
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Delete Other Expenses
  
  
  
  API: Individuals Expenses
}
