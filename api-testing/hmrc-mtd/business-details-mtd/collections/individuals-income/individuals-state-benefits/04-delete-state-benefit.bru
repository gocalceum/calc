meta {
  name: Delete State Benefit
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/individuals/state-benefits/{nino}/{taxYear}/{benefitId}
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Delete State Benefit
  
  
  
  API: Individuals State Benefits
}
