meta {
  name: Amend Loss Claims Order
  type: http
  seq: 11
}

put {
  url: {{base_url}}/individuals/losses/{nino}/loss-claims/order/{taxYearClaimedFor}
  body: json
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Amend Loss Claims Order
  
  
  
  API: Individual Losses
}
