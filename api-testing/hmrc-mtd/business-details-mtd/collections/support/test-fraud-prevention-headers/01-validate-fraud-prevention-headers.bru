meta {
  name: Validate fraud prevention headers
  type: http
  seq: 1
}

get {
  url: {{base_url}}/test/fraud-prevention-headers/validate
  auth: none
}

headers {
  Accept: application/json
  Authorization: Bearer {{access_token}}
  Gov-*: {{Gov-*}}
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Validate fraud prevention headers
  
  This resource validates fraud prevention headers submitted with this HTTP request.
  
  API: Test Fraud Prevention Headers
}
