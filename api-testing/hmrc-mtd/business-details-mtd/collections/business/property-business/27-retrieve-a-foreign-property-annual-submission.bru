meta {
  name: Retrieve a Foreign Property Annual Submission
  type: http
  seq: 27
}

get {
  url: {{base_url}}/individuals/business/property/foreign/{nino}/{businessId}/annual/{taxYear}
  auth: none
}

tests {
  test("Should return success", function() {
    expect(res.status).to.be.oneOf([200, 201, 204]);
  });
}

docs {
  ## Retrieve a Foreign Property Annual Submission
  
  
  
  API: Property Business
}
